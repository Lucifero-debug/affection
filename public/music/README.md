# Music

Drop one `.mp3` here (default name: `song.mp3`) and it becomes the
tap-to-play track in the bottom-right corner.

Nothing autoplays — she has to press it.

To change the filename or the label, edit `music` in `app/content.ts`.
To remove the player entirely, set `music.enabled = false`.
